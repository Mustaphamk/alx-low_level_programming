#include "main.h"

/**
 * _abs - compute the absolute value of integer
 * @n: the integer to be computed
 *
 * Return: the absolut value of integer
 */
int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
